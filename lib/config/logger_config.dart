/// Configure logging for the application
class LoggerConfig {
  static void configure() {
    // Initialize logging configuration
    // Console filtering is handled in web/index.html
  }
}
void Function(String) overridePrint(void Function(String) printFunction) {
  return printFunction;
}
